{
  "mcpServers": {
    "codex-tdd": {
      "command": "codex",
      "args": [
        "mcp-server",
        "-c",
        "skills.config=[{enabled=true, path=\"~/.claude/plugins/cache/claude-dev/skills/tdd-dev\"}]"
      ],
      "description": "Codex MCP server with TDD skill enabled"
    }
  }
}
